<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sample.common.dao.mapper.UserMapper">
	
	<!-- ユーザー登録 -->
	<insert id="insertUser" parameterType="sample.common.dao.entity.User">
		INSERT INTO users (username, password, created_at, updated_at)
    	VALUES (#{username}, #{password}, #{createdAt}, #{updatedAt})
	</insert>
	
	<!-- ユーザー検索 -->
	<select id="userSearch" parameterType="sample.common.dao.entity.User" resultType="sample.common.dao.entity.User">
		SELECT id, username, password
		FROM users
		WHERE username = #{username}
		AND password = #{password}
	</select>

</mapper>